language: python
python: "3.8"
install:
    - pipenv install --dev
script:
    - pipenv run python -m coverage run --source='.' -m pytest && pipenv run pylint --errors-only
after_success:
    - pip install codecov
    - codecov
